
const SecondTextDisplay = ({ secondText }) => {
    return (
        <div className="second_text">
            <p className="displayed_text">{secondText}</p>
        </div>
    )
}

export default SecondTextDisplay